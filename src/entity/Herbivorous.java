package entity;

public abstract class Herbivorous extends Animal  {
    public Herbivorous(double weight, int maxSpeed, double maxPerCell, double maxSatiety, double foodNeeded){
        super(weight, maxSpeed, maxPerCell, maxSatiety, foodNeeded);
    }

}
